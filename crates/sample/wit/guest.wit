package component:guest-wit;

world example {
    
    use wasvy:ecs/app.{query, schedule, system, query-for};
    use game:components/components.{health};

    
    export heal-system: func(query: borrow<query>);
    export pct-system: func(query: borrow<query>);

    
    include wasvy:ecs/guest;
}