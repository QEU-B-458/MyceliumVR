package game:components;

interface components {
  use wasvy:ecs/app.{component};

  /// wasvy:type-path=MyceliumVR::components::Health
  resource health {
    constructor(component: component);
    heal: func(amount: f32);
    pct: func() -> f32;
  }
}

world host {
  import components;
}
